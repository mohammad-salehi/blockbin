version: '3'
services:
  panta_front:
    image: $IMAGE_NAME
    container_name: blockbin_front
    ports:
      - "4001:4173"
    env_file:
      - $PROJECT_ENV_FILE
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "10"

    restart: always

